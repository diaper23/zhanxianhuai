---
身份风险_严重暴露: |-
  <%_/* 覆盖 :line: "身份风险严重" */
  if ((plot("用户.身份风险") === "严重") && !hitDone('E006_Identity_Risk_Raised')) {
    markHit('覆盖', '身份风险_严重暴露', `<span style="display:none;">[覆盖剧情：身份风险_严重暴露]</span>
    !!!最高指令：必须让一个高智角色对<user>的异常产生实质追问。
    追问应围绕“出处、师承、典籍、梦兆、幕后高人”，不能直接说穿越。`);
  } _%>
