---
暗示_层头: |-
  <%
  const ids = getvar('stat_data.剧情要点.本轮命中.暗示', { defaults: [], noCache: true }) || [];
  if (ids.length) {
  %>
  执行指令：在当前回复的【环境描写】或【路人互动】中嵌入对目标剧情的隐晦侧写。严禁直接点破身份或事件全貌，必须做到"草蛇灰线，伏脉千里"。
  需暗示剧情:
  <% for (const id of ids) { %>- <%- id %>
  <% } %>
  <% } %>
