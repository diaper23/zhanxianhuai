---
覆盖_层头: |-
  <%
  const ids = getvar('stat_data.剧情要点.本轮命中.覆盖', { defaults: [], noCache: true }) || [];
  if (ids.length) {
  %>
  执行指令：【剧情劫持警告】当前回合必须强行转移叙事焦点！
  剧情原则：
    - 【强制转折】制造合理突发事件夺取叙事主导权
    - 【超展开禁令】突发感应来自【时机的精巧】，而非逻辑的崩坏
    - 【东晋时空禁令】严禁出现超越东晋信息传递速度的情节
  需覆盖剧情:
  <% for (const id of ids) { %>- <%- id %>
  <% } %>
  <% } %>
