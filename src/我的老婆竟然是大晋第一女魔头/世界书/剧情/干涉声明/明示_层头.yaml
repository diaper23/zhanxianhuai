---
明示_层头: |-
  <%
  const ids = getvar('stat_data.剧情要点.本轮命中.明示', { defaults: [], noCache: true }) || [];
  if (ids.length) {
  %>
  执行指令：目标已近在咫尺。必须通过强烈的【五感描写】进行贴脸预示，营造下一秒即将相遇或爆发的紧迫感，但【保留最后一丝悬念】。
  需明示剧情:
  <% for (const id of ids) { %>- <%- id %>
  <% } %>
  <% } %>
